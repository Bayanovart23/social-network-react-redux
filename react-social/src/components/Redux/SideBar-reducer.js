let initialState = {
    contacts: [
        {id: 1, name: 'Artur'},
        {id: 2, name: 'Inessa'},
        {id: 3, name: 'Misha'},
    ]
};

let sideBarReducer = (state = initialState, action) => {

    return state;
}

export default sideBarReducer;